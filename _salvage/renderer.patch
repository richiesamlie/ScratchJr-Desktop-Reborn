diff --git a/src/app/appEntry.js b/src/app/appEntry.js
index 94f8395..0352329 100755
--- a/src/app/appEntry.js
+++ b/src/app/appEntry.js
@@ -34,22 +34,20 @@ const pageEntries = {
  * (e.g. from the lobby) has no side effects.
  */
 export function bootApp () {
-	if (!window.scratchjr) {
-		throw new Error('ScratchJr: preload bridge missing');
-	}
-	const ipc = window.scratchjr;
-
 	window.onload = () => loadPage(document.body.dataset.scratchjrPage || window.scratchJrPage || '').catch((err) => console.error('loadPage failed:', err)); // eslint-disable-line no-console
 
 	// Close handshake lives here (not in the editor chunk) so quitting from any
 	// page acks immediately; the editor chunk saves first via window.ScratchJr.
-	ipc.onAppClose(function () {
-		if (window.ScratchJr && window.ScratchJr.saveProject) {
-			window.ScratchJr.saveProject(null, function () { ipc.sendAppClosedAcked(); });
-		} else {
-			ipc.sendAppClosedAcked();
-		}
-	});
+	if (typeof window !== 'undefined' && window.scratchjr) {
+		const ipc = window.scratchjr;
+		ipc.onAppClose(function () {
+			if (window.ScratchJr && window.ScratchJr.saveProject) {
+				window.ScratchJr.saveProject(null, function () { ipc.sendAppClosedAcked(); });
+			} else {
+				ipc.sendAppClosedAcked();
+			}
+		});
+	}
 }
 
 
diff --git a/src/app/css/base.css b/src/app/css/base.css
index 9d0db61..9c2660b 100755
--- a/src/app/css/base.css
+++ b/src/app/css/base.css
@@ -17,7 +17,6 @@ html {
 body {
     width: 100%;
     height: 100%;
-    min-width: 1020px;
     overflow-y: hidden;
     overflow-x: hidden;
 }
diff --git a/src/app/css/editor.css b/src/app/css/editor.css
index 735d314..f5b4ab1 100755
--- a/src/app/css/editor.css
+++ b/src/app/css/editor.css
@@ -1,8 +1,6 @@
 div.frame {
     width: 100%;
     height: 100%;
-    min-width: 766px;
-    min-height: 745px;
     position: relative;
     user-select: none;
 }
diff --git a/src/app/css/librarymodal.css b/src/app/css/librarymodal.css
index f6a6b8d..1358554 100755
--- a/src/app/css/librarymodal.css
+++ b/src/app/css/librarymodal.css
@@ -6,15 +6,8 @@
     background: url('../assets/libbkg.png');
     background-size: 100%;
     display: none;
-   
     width: 100%;
     height: 100%;
-    min-width: 766px;
-    min-height: 745px;
-   
-    
-    
-    user-select: none;
     user-select: none;
 }
 
diff --git a/src/app/css/lobby.css b/src/app/css/lobby.css
index 66016d3..ea7d553 100755
--- a/src/app/css/lobby.css
+++ b/src/app/css/lobby.css
@@ -9,11 +9,8 @@ div.frame {
     position: relative;
     width: 100%;
     height: 100%;
-    min-width: 766px;
-    min-height: 745px;
     background: #E8E3CA;
     overflow: hidden;
-    
     user-select: none;
     background-image: linear-gradient(to bottom,rgba(255, 255, 255, 0.70), rgba(255, 255, 255, 0.70) 10%, rgba(255, 255, 255, 0.70) 90%, rgba(255, 255, 255, 0));
 }
@@ -34,7 +31,6 @@ div.frame {
     border-top: 1px solid #E8E3CA;
     box-shadow: 0px ${1 * scaleMultiplier}px ${1 * scaleMultiplier}px #fff;
     width: 100%;
-    min-width: 800px;
 }
 
 #topbar ul.topbar-nav {
diff --git a/src/app/css/start.css b/src/app/css/start.css
index d2f1f07..2109e02 100755
--- a/src/app/css/start.css
+++ b/src/app/css/start.css
@@ -13,30 +13,31 @@ html, body, {
 div.frame {
     width: 100%;
     height: 100%;
-    min-width: 766px;
-    min-height: 745px;
     position: relative;
     background: #35A8E0;
     overflow: hidden;
-    
 }
 
 *.catlogo {
     position: absolute;
-    top: 127px;
-    left: 284px;
+    top: 15%;
+    left: 25%;
     margin: 0px;
-    width: 468px;
-    height: 300px;
+    width: 50%;
+    height: 45%;
     z-index: 10;
     background: url('../assets/start/catlogo.png');
+    background-repeat: no-repeat;
+    background-size: contain;
+    background-position: center;
 }
 
 *.credits {
     position: absolute;
     bottom: 0px;
     width: 100%;
- height: 400px;
+    height: 35%;
+    max-height: 300px;
     background: url('../assets/start/bottom.png');
     background-position: center bottom;
     background-repeat: no-repeat;
@@ -48,10 +49,12 @@ div.frame {
     position: absolute;
     bottom: 10px;
     width: 100%;
-    height: 350px;
+    height: 30%;
+    max-height: 250px;
     background: url('../assets/start/creditsText.png');
     background-position: center center;
     background-repeat: no-repeat;
+    background-size: contain;
     z-index: 21;
 }
 
@@ -62,15 +65,15 @@ div.frame {
 
 *.jrlogo {
     position: absolute;
-    top: 26%;
+    top: 15%;
     width: 100%;
     margin: 0px;
-    height: 16%;
+    height: 32%;
     z-index: 10;
     background: url('../assets/start/ScratchJrLogo.svg');
     background-position: center center;
     background-repeat: no-repeat;
-    background-size: auto 100%;
+    background-size: contain;
 }
 
 *.purple {
@@ -122,17 +125,15 @@ div.frame {
 
 .gettings {
     position: absolute;
-    top: 65%;
-    left: 59%;
+    top: 55%;
+    left: 54%;
     margin: 0px;
-    width: 17%;
-    height: 22%;
-    min-width: 150px;
-    min-height: 150px;
+    width: 22%;
+    height: 32%;
     background: url('../assets/start/helpOn.svg');
     background-repeat: no-repeat;
-    background-size: auto 100%;
-    background-position-x: center;
+    background-size: contain;
+    background-position: center;
     -webkit-tap-highlight-color: transparent;
     z-index: 1000;
 }
@@ -140,8 +141,8 @@ div.frame {
 .gettings:active {
     background: url('../assets/start/helpOff.svg');
     background-repeat: no-repeat;
-    background-size: auto 100%;
-    background-position-x: center;
+    background-size: contain;
+    background-position: center;
 }
 
 .gettings.hide {display: none;}
@@ -156,8 +157,8 @@ div.frame {
     right: 25px;
     background: url('../assets/lobby/gearOff.svg');
     background-repeat: no-repeat;
-    background-size: auto 100%;
-    background-position-x: center;
+    background-size: contain;
+    background-position: center;
     -webkit-tap-highlight-color: transparent;
     z-index: 1000;
     width: 60px;
@@ -166,17 +167,15 @@ div.frame {
 
 .startcode {
     position: absolute;
-    top: 65%;
+    top: 55%;
     left: 24%;
     margin: 0px;
-    width: 17%;
-    height: 22%;
-    min-width: 150px;
-    min-height: 150px;
+    width: 22%;
+    height: 32%;
     background: url('../assets/start/houseOn.svg');
     background-repeat: no-repeat;
-    background-size: auto 100%;
-    background-position-x: center;
+    background-size: contain;
+    background-position: center;
     -webkit-tap-highlight-color: transparent;
     z-index: 1000;
 }
@@ -187,8 +186,8 @@ div.frame {
 .startcode:active {
     background: url('../assets/start/houseOff.svg');
     background-repeat: no-repeat;
-    background-size: auto 100%;
-    background-position-x: center;
+    background-size: contain;
+    background-position: center;
 }
 
 .rays.hide, .catface.hide, .jrlogo.hide, .frame.hide {display: none;}
diff --git a/src/app/src/entry/home.ts b/src/app/src/entry/home.ts
index 858072b..1189e88 100755
--- a/src/app/src/entry/home.ts
+++ b/src/app/src/entry/home.ts
@@ -4,7 +4,15 @@ import PlatformBridge from '../platform/PlatformBridge';
 import Lobby from '../lobby/Lobby';
 
 export function homeMain () {  // eslint-disable-line import/prefer-default-export
-    gn('logotab')!.onmousedown = homeGoBack;
+    const logotab = gn('logotab');
+    if (logotab) {
+        logotab.onmousedown = homeGoBack;
+        logotab.ontouchend = function (e) {
+            e.preventDefault();
+            e.stopPropagation();
+            homeGoBack();
+        };
+    }
     homeStrings();
     PlatformBridge.getsettings(doNext);
     function doNext (str: string) {
diff --git a/src/app/src/entry/index.ts b/src/app/src/entry/index.ts
index 31e903c..0ef9bb4 100755
--- a/src/app/src/entry/index.ts
+++ b/src/app/src/entry/index.ts
@@ -4,9 +4,20 @@ import PlatformBridge from '../platform/PlatformBridge';
 import Localization from '../utils/Localization';
 import AppUsage from '../utils/AppUsage';
 
+function bindTap (id: string, handler: (e: any) => void) {
+    const el = gn(id);
+    if (!el) return;
+    el.onmousedown = handler;
+    el.ontouchend = function (e) {
+        e.preventDefault();
+        e.stopPropagation();
+        handler(e);
+    };
+}
+
 export function indexMain () { // eslint-disable-line import/prefer-default-export
-    gn('gettings')!.onmousedown = indexGettingstarted;
-    gn('startcode')!.onmousedown = indexGohome;
+    bindTap('gettings', indexGettingstarted);
+    bindTap('startcode', indexGohome);
     ScratchAudio.init();
     var urlvars = getUrlVars();
     if (urlvars.back) {
@@ -20,14 +31,14 @@ export function indexMain () { // eslint-disable-line import/prefer-default-expo
         gn('startButton')!.textContent = Localization.localize('PBS_START');
         gn('gettings')!.textContent = Localization.localize('PBS_HOW_TO');
 
-        gn('startButton')!.onmousedown = indexGohome;
-        gn('pbschars')!.onmousedown = indexGohome;
+        bindTap('startButton', indexGohome);
+        bindTap('pbschars', indexGohome);
 
-        gn('topbar-moreapps')!.onmousedown = indexMoreApps;
-        gn('topbar-settings')!.onmousedown = indexGoSettings;
-        gn('topbar-info')!.onmousedown = indexInfo;
+        bindTap('topbar-moreapps', indexMoreApps);
+        bindTap('topbar-settings', indexGoSettings);
+        bindTap('topbar-info', indexInfo);
     } else {
-        gn('gear')!.onmousedown = indexGoSettings;
+        bindTap('gear', indexGoSettings);
     }
 
     setTimeout(function () {
@@ -79,6 +90,7 @@ function indexLoadStart (afterUsage?: boolean) {
         gn('blueguy')!.className = 'blue hide';
         gn('redguy')!.className = 'red hide';
         gn('gear')!.className = 'gear show';
+        bindTap('gear', indexGoSettings);
         
         if (afterUsage) {
             gn('catface')!.className = 'catface show';
@@ -92,6 +104,8 @@ function indexLoadStart (afterUsage?: boolean) {
     }
     gn('gettings')!.className = 'gettings show';
     gn('startcode')!.className = 'startcode show';
+    bindTap('gettings', indexGettingstarted);
+    bindTap('startcode', indexGohome);
     document.onmousemove = function (e) {
         e.preventDefault();
     };
@@ -119,10 +133,10 @@ function indexLoadUsage() {
     gn('usageHome')!.className = 'usageHome show';
     gn('usageOther')!.className = 'usageOther show';
     gn('usageNoanswer')!.className = 'usageNoanswer show';
-    gn('usageSchool')!.onmousedown = indexSetUsage;
-    gn('usageHome')!.onmousedown = indexSetUsage;
-    gn('usageOther')!.onmousedown = indexSetUsage;
-    gn('usageNoanswer')!.onmousedown = indexSetUsage;
+    bindTap('usageSchool', indexSetUsage);
+    bindTap('usageHome', indexSetUsage);
+    bindTap('usageOther', indexSetUsage);
+    bindTap('usageNoanswer', indexSetUsage);
 
 }
 
diff --git a/src/app/src/lobby/Home.ts b/src/app/src/lobby/Home.ts
index 2d4c340..f8a3763 100755
--- a/src/app/src/lobby/Home.ts
+++ b/src/app/src/lobby/Home.ts
@@ -33,6 +33,8 @@ export default class Home {
         div.setAttribute('id', 'scrollarea');
         frame.onmousedown = Home.handleTouchStart;
         frame.onmouseup = Home.handleTouchEnd;
+        frame.ontouchstart = Home.handleTouchStart as unknown as (this: GlobalEventHandlers, ev: TouchEvent) => any;
+        frame.ontouchend = Home.handleTouchEnd as unknown as (this: GlobalEventHandlers, ev: TouchEvent) => any;
         Home.installSjrDrop();
         Home.displayYourProjects();
     }
@@ -66,6 +68,7 @@ export default class Home {
         }
         function holdit () {
             frame.onmousemove = Home.handleMove;
+            frame.ontouchmove = Home.handleMove as unknown as (this: GlobalEventHandlers, ev: TouchEvent) => any;
             var repeat = function () {
                 if (Home.actionTarget) {
                     Home.showProjectControls(Home.actionTarget);
@@ -113,6 +116,7 @@ export default class Home {
             return;
         }
         frame.onmousemove = null;
+        frame.ontouchmove = null;
         if (timeoutEvent) {
             clearTimeout(timeoutEvent);
         }
diff --git a/src/app/src/lobby/Lobby.ts b/src/app/src/lobby/Lobby.ts
index ddac360..54142d5 100755
--- a/src/app/src/lobby/Lobby.ts
+++ b/src/app/src/lobby/Lobby.ts
@@ -47,46 +47,57 @@ export default class Lobby {
             gn('settings')!.style.visibility = 'hidden';
         }
 
-        gn('hometab')!.onmousedown = function () {
+        function bindTab (id: string, action: () => void) {
+            const el = gn(id);
+            if (!el) return;
+            el.onmousedown = action;
+            el.ontouchend = function (e) {
+                e.preventDefault();
+                e.stopPropagation();
+                action();
+            };
+        }
+
+        bindTab('hometab', function () {
             if (gn('hometab')!.className != 'home on') {
                 Lobby.setPage('home');
             }
-        };
-        gn('helptab')!.onmousedown = function () {
+        });
+        bindTab('helptab', function () {
             if (gn('helptab')!.className != 'help on') {
                 Lobby.setPage('help');
             }
-        };
-        gn('booktab')!.onmousedown = function () {
+        });
+        bindTab('booktab', function () {
             if (gn('booktab')!.className != 'book on') {
                 Lobby.setPage('book');
             }
-        };
-        gn('geartab')!.onmousedown = function () {
+        });
+        bindTab('geartab', function () {
             if (gn('geartab')!.className != 'gear on') {
                 Lobby.setPage('gear');
             }
-        };
-        gn('abouttab')!.onmousedown = function () {
+        });
+        bindTab('abouttab', function () {
             if (gn('abouttab')!.className != 'tab on') {
                 Lobby.setSubMenu('about');
             }
-        };
-        gn('interfacetab')!.onmousedown = function () {
+        });
+        bindTab('interfacetab', function () {
             if (gn('interfacetab')!.className != 'tab on') {
                 Lobby.setSubMenu('interface');
             }
-        };
-        gn('painttab')!.onmousedown = function () {
+        });
+        bindTab('painttab', function () {
             if (gn('painttab')!.className != 'tab on') {
                 Lobby.setSubMenu('paint');
             }
-        };
-        gn('blockstab')!.onmousedown = function () {
+        });
+        bindTab('blockstab', function () {
             if (gn('blockstab')!.className != 'tab2 on') {
                 Lobby.setSubMenu('blocks');
             }
-        };
+        });
     }
 
     static setPage (page: string) {
diff --git a/src/app/src/platform/PlatformBridge.ts b/src/app/src/platform/PlatformBridge.ts
index 5bd3fc8..2a25fd0 100644
--- a/src/app/src/platform/PlatformBridge.ts
+++ b/src/app/src/platform/PlatformBridge.ts
@@ -43,9 +43,10 @@ export default class PlatformBridge {
             return;
         }
 
-        // Android device fallback (if running in mobile WebView)
-        if (typeof AndroidInterface !== 'undefined') {
-            hostInterface = AndroidInterface as unknown as ScratchJrBridge;
+        // Android device interface (injected on window.AndroidInterface or window.Android)
+        const android = (typeof window !== 'undefined' && ((window as any).AndroidInterface || (window as any).Android)) || (typeof AndroidInterface !== 'undefined' ? AndroidInterface : null);
+        if (android) {
+            hostInterface = android as unknown as ScratchJrBridge;
             if (fcn) {
                 fcn();
             }
@@ -53,18 +54,18 @@ export default class PlatformBridge {
         }
 
         // Desktop / iOS host bridge - might not be loaded yet
-        if (typeof (window.tablet) != 'object') {
-            // Come back in 100ms
-            setTimeout(function () {
-                PlatformBridge.waitForInterface(fcn);
-            }, 100);
-        } else {
-            // All set to run commands
+        if (typeof window !== 'undefined' && typeof (window.tablet) == 'object') {
             hostInterface = window.tablet;
             if (fcn) {
                 fcn();
             }
+            return;
         }
+
+        // Retry in 50ms
+        setTimeout(function () {
+            PlatformBridge.waitForInterface(fcn);
+        }, 50);
     }
 
     // Database functions
