/** Stage geometry shared by the runtime primitives and the grid overlay. */

/** Grid cell size in stage pixels (see ui/Grid.ts drawing code). */
export const GRID_SIZE = 24;

/** Stage dimensions in stage pixels. */
export const STAGE_WIDTH = 480;
export const STAGE_HEIGHT = 360;
