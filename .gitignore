node_modules
out
build
src/app/dist
.DS_Store
buglist.txt
src/debug.log
*.log
# CI build artifacts (zip + checksum files generated during release)
ScratchJr-*.zip
ScratchJr-*.zip.sha256

# Internal migration tracking (never published)
MIGRATION.md

# Android build outputs — everything under android/app/src/main/assets/ is
# generated by scripts/build-android-assets.js (www/ pages + assets/hostClient.js)
# and must never be committed (the mistake that killed the first port; CI guards it).
android/app/src/main/assets/
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
*.apk
*.aab

# Web PWA build distribution
dist-web/

